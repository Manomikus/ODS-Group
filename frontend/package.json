{
  "name": "ods-group-static",
  "version": "1.0.0",
  "private": true,
  "description": "Static-file server for ODS Group marketing site (root at /app).",
  "scripts": {
    "start": "serve -s /app -l tcp://0.0.0.0:3000 --no-clipboard --no-port-switching"
  },
  "dependencies": {
    "serve": "14.2.4"
  }
}
